class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        #Create array with start and n
        answer = 0
        nums = []
        for n in range(n):
            nums.append(start + n * 2)
        for n in nums:
            answer = answer ^ n
        return answer
